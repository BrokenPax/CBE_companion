Latest standard-JS GitHub Pages bundle built from the current canvas state. Overwrite the old root files in your repo and push. Clear site data or reinstall the home-screen app if a cached version sticks around.